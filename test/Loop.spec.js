import chai from 'chai'
import Loop from '../src/Loop'

chai.should()

describe('Loop', () => {
  it('Should be instantiatable with a store', () => {
    const b = new Loop()

    // b.isEnabled.should.equal(false)
  })
})
